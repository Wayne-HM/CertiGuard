services:
  - type: web
    name: certiguard-api
    runtime: docker
    plan: starter
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.0
    # The build and start commands are handled by the Dockerfile
